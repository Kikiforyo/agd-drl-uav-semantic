# This file makes the ``envs`` directory a Python package. It is
# intentionally left empty but is required so that modules like
# ``envs.covert_semantic_env`` can be imported from scripts in other
# directories.